<%= render 'layouts/sidebar_sorting' %>

<br/>
<!--<ul>
<% @statusupdates.each do |su| %>
	<li>
		<%= link_to image_tag(su.project.icon_url), su.project, :class=>"pic" %><%= su.project.title %><br/>
		<%= su.content %> <br/>(<%= su.user.name %>)
	</li><br/>
<% end %>
</ul>-->

<%= render 'sidebar' %>

<!-- OVERLAY -->
<div id="overlayNotifications" class="overlay">
	<h1>Notifications</h1>
	<a class="close" id="close"><%= image_tag 'close.png' %></a>
	<div class="content scroll">
    <ul>
	  <% @notifications.each do |n| %>
      <li <% if n.isNew %>class="new"<% end %>>
        <%= link_to image_tag('close-notice.png'), n, :method=>:delete, :class=>"delete_notification", :remote=>true %>
	  	  <p><%= n.content %></p>
        <sub class="date"><%= fdate n.created_at, true, true %></sub>
	    </li>
    <% end %>
    </ul>
	  <div class="clean"></div>
	</div>
</div>