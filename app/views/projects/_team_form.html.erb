<div class="inline">
  <%= form_for @project, :remote => true do |f| %>
    <%= hidden_field_tag 'field', 'team' %>
    <%= render 'dialog' %>
      <div id="memberlist">
        <% @project.has_members.each do |r| %>
          <div class="member no<%= r.id %>">
            <%= link_to image_tag(avatar_url(r.user, 40)), r.user, :class=>"pic" %>
            <p>
              <% if (r != @owner) %>
                <%= link_to image_tag("delete.png"), "javascript:memberDelete('#{r.id}');", :class=>"right" %>
              <% end %>
              <b><%= r.user.name %></b><br><%= text_field_tag "roles[]",r.job, :class=>"editjob" %>
            </p>
          </div>
        <% end %>	
        <div class="hide">
        <% @project.has_members.each do |r| %>
           <%= check_box_tag "project[role_ids][]", r.id, @project.has_members.include?(r), :class=>"hiddenroles" %>
          <% end %>
        </div>
      </div>
  <%= render 'submit_buttons' %>
  <% end %>
</div>